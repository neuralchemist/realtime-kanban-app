// mui
import { styled } from "@mui/material/styles";
import Box from "@mui/material/Box";

export const StyledProjectSection = styled(Box)(() => ({
  width: "100%",
  //   ----DEBUG----
  // border: "1px solid pink",
}));

export const StyledProjectTop = styled(Box)(() => ({
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
  //   ----DEBUG----
  // border: "1px solid pink",
}));
